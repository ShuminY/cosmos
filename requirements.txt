# Core
numpy>=1.26,<2.0
scipy>=1.11
pillow>=10.0

# Deep Learning
torch>=2.2
torchvision>=0.17
transformers>=4.40
huggingface-hub>=0.23

# Document Processing
pypdf>=4.0
python-docx>=1.1
python-pptx>=1.0
openpyxl>=3.1
pandas>=2.2
pdf2image
PyMuPDF>=1.24
pytesseract
ezdxf>=1.1  # DWG/DXF CAD 图纸解析
matplotlib>=3.7  # ezdxf 渲染 DXF 为图片的后端（兜底用）

# CAD DWG 预览外部依赖（必须安装）：
#   ODA File Converter + LibreOffice
#
# macOS 安装方式：
#   1. ODA File Converter: https://cad-manager-odafileconverter.com
#      安装到 /Applications/ODAFileConverter.app（无空格版）
#   2. LibreOffice: brew install --cask libreoffice 或官网下载
#
# 工作链路：DWG → (ODA) → DXF R12 → (LibreOffice) → PDF → (PyMuPDF) → PNG

# OCR Support
# poppler-utils (system)
# tesseract-ocr (system)
rapidocr_onnxruntime>=1.3.24  # 图纸标题栏图名/图号提取（src/ocr_worker.py 子进程调用）

# Database
sqlalchemy>=2.0
bcrypt>=4.1

# UI
streamlit>=1.40
plotly>=5.20
opencv-python>=4.9
streamlit-drawable-canvas>=0.9  # 人工批注在图上拖拽画框选定位置

# LLM
openai>=1.0

# Utilities
pydantic>=2.7
rich>=13.7
typer>=0.12
pyyaml>=6.0
tqdm>=4.66
requests>=2.31

# Optional (for advanced features)
# open3d>=0.18
# trimesh>=4.3
